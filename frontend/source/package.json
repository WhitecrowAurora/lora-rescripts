{
  "name": "sd-rescripts-frontend-source",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "smoke:training": "esbuild scripts/trainingRouteSmoke.ts --bundle --platform=node --format=esm --define:import.meta.env={} --outfile=.smoke/trainingRouteSmoke.mjs && node .smoke/trainingRouteSmoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.10"
  }
}
