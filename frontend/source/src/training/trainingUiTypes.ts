export type UtilityTone = "idle" | "success" | "warning" | "error";

export type PanelName = "history" | "presets" | "recipes";

export type TrainingStateLike = {
  values: Record<string, unknown>;
};
